use std::process::Command;

fn get_arch() -> arch{
    struct arch {
        is_x386: bool,
        is_x64: bool
    }
    impl arch{
        fn new() -> arch {
            if
        }
    }
}
fn get_version() -> version {
    struct version {
        common: str,
        major: i32,
        minor: i32
    }
    impl version{
        fn new() -> version {

        }
    }
}
fn get_vendor -> vendor{
    struct vendor{
        name: str
    }
    impl vendor{
        fn new() -> vendor {

        }
    }
}
